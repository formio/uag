Form data collected successfully!

**All required fields have been collected!**

<%= dataSummary %>

Next steps:
1. Use the `get_optional_fields` tool to check if there are optional fields and ask the user if they want to fill any
2. If no optional fields or user declines, use `confirm_form_submission` tool to show summary and get confirmation